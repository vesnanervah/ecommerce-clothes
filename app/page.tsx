"use client";

import NavBar from "./lib/nav/ui/nav-bar";

export default function Home() {
  return <>
        <NavBar onBurgerClick={() => {}} onSearchClick={() => {}} onProfileClick={() => {}} onFavoriteClick={() => {}} onCartClick={() => {}}></NavBar>

  </>
}
